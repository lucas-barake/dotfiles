{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/bin.ts --compile --outfile bin/dotai",
    "global": "bun run src/bin.ts global",
    "project": "bun run src/bin.ts project",
    "test": "vitest run",
    "sync": "bun run src/bin.ts project"
  },
  "dependencies": {
    "@effect/platform-bun": "beta",
    "effect": "beta"
  },
  "devDependencies": {
    "@effect/platform-node": "beta",
    "@effect/vitest": "beta",
    "vitest": "latest",
    "@types/bun": "latest"
  }
}
