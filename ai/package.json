{
  "private": true,
  "type": "module",
  "scripts": {
    "agents": "bun run src/bin.ts agents",
    "build": "bun build ./src/bin.ts --compile --outfile bin/dotai",
    "test": "vitest run",
    "sync": "bun run src/bin.ts sync"
  },
  "dependencies": {
    "@effect/platform-bun": "beta",
    "effect": "beta"
  },
  "devDependencies": {
    "@effect/platform-node": "beta",
    "@effect/vitest": "beta",
    "vitest": "latest",
    "@types/bun": "latest"
  }
}
